{
  "tasks": {
    "dev": "deno run --watch src/main.ts",
    "build": "deno compile --output bin/claudinator src/main.ts",
    "test": "deno test --coverage=coverage",
    "compile": "deno compile --output bin/claudinator src/main.ts",
    "install": "deno install --name claudinator --allow-env --allow-read --allow-write --allow-net --allow-run src/main.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "@std/cli": "jsr:@std/cli@^1.0.20"
  }
}